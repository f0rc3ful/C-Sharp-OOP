namespace ShoppingSpree
{
    class Program
    {
        List<string> allPeople = Console.ReadLine().Split(";").ToList();
        List<Person> people = new List<Person>();
        List<string> allProducts = Console.ReadLine().Split(";").ToList();
    }
}
